<% can.each(columns, function(column, i) { %>
	<div class="column-util-element" columnID="<%= column.oid %>" columName="<%= column.title %>" oid="<%= column.oid %>"></div>        
<% }); %>

<div style="clear: both;"></div>